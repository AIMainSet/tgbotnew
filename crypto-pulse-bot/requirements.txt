# Основные зависимости
aiogram==3.10.0
python-dotenv==1.0.1

# База данных (SQLite встроен в Python, поэтому только драйвер)
# asyncpg==0.29.0    <-- НЕ НУЖЕН для SQLite!
# psycopg2-binary==2.9.9  <-- НЕ НУЖЕН для SQLite!

# Для работы с CryptoBot API
aiocryptopay==0.4.8  # Асинхронная библиотека для CryptoPay
aiohttp==3.9.5      # HTTP-клиент (нужен aiocryptopay)
aiofiles==23.2.1

# Дополнительные (для следующих этапов)
redis==5.0.1         # Для очередей уведомлений
apscheduler==3.10.4  # Для планировщика задач
ccxt==4.2.94         # Для работы с биржами
pandas==2.1.4        # Для анализа данных
numpy==1.26.3        # Зависимость pandas
requests==2.31.0     # Для HTTP-запросов (если нужно)
